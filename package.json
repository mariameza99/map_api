{
  "name": "map_api",
  "version": "1.0.0",
  "description": "APi con express bajo CBD",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "serve": "nodemon App.js",
    "undomigrations": "npx sequelize-cli db:migrate:undo:all",
    "migrations": "npx sequelize-cli db:migrate",
    "remigrate": "npx sequelize-cli db:migrate:undo:all && npx sequelize-cli db:migrate",
    "seedAll": "npx sequelize-cli db:seed:all"
  },
  "author": "Maria Concepcion Meza Manriquez",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^4.0.1",
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "jsonwebtoken": "^8.5.1",
    "mysql": "^2.18.1",
    "mysql2": "^2.1.0",
    "sequelize": "^5.21.5"
  },
  "devDependencies": {
    "sequelize-cli": "^5.5.1"
  }
}
